import { Typography } from '@mui/material';
import React from 'react';

const Blog = () => {
    return (
        <div style={{backgroundColor: "#070640"}}>
            <Typography variant="h4" gutterBottom component="div" sx={{color: 'white', textAlign: 'center', paddingTop: '70px', paddingBottom: '50px'}}>
                coming soon
            </Typography>
        </div>
    );
};

export default Blog;